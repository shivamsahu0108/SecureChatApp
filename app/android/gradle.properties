org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Workaround for Windows builds when sources are on different drive roots
# (e.g. project/build on D: but Flutter Pub cache on C:), which can break
# Gradle/Kotlin path relativization during task configuration.
kotlin.incremental=false
